/** MASTER_PLAN §6.2 — tweak here, not in screens. */
export const evaluationConfig = {
  greenMinRoutineRate: 0.5,
  greenMinSpheres: 2,
  yellowMinRoutineRate: 0.35,
  workImbalanceMinWorkDone: 3,
} as const;
