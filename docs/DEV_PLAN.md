# План разработки SteadyWeek (актуальный)

Продуктовая спека (сферы, сущности, XP, огонёк) остаётся в [`MASTER_PLAN.md`](./MASTER_PLAN.md).  
Этот файл — **как строим сейчас**: стек, слои, порядок фич. Если конфликт со старыми фазами Flutter/Supabase — верить **этому** документу.

---

## 0. Зафиксированный стек

| Слой | Выбор | Зачем |
|------|--------|--------|
| Клиент | Expo / React Native + TypeScript (`ReactNative-version/frontend`) | один код, привычный стек |
| Локально на устройстве | **SQLite** | работает без интернета |
| Облако / источник правды между устройствами | **PostgreSQL на Neon** | бесплатный tier не усыпляет так, как типичный free Supabase; уже есть опыт |
| API | **NestJS** (`ReactNative-version/backend`) | auth, CRUD, sync, Groq-прокси, позже MCP |
| Auth | свой (Nest + JWT или sessions) поверх Postgres | **без Supabase** |
| AI в приложении | лёгкий ассистент → Nest → **Groq** (ключ только на сервере) | шаблоны, если нет сети / 429 |
| AI за компом | MCP → тот же Nest API | Cursor правит расписание |
| Flutter `flutter-version/` | архив / референс UI и формул | не развиваем параллельно |

**Не делаем:** MongoDB в этом проекте, IAP, серверный push в первой рабочей версии, live-merge двух устройств с ручным слиянием.

### SQLite и Postgres — не «или», а два этажа

```
Телефон / Web (офлайн)
  SQLite  ← UI всегда читает отсюда
     │
     │  есть сеть: push/pull изменений
     ▼
  NestJS
     ▼
  Neon PostgreSQL  ← облачная правда, бэкап, Web с другого ПК, MCP
```

Синхронизация **возможна и так и задумана**. Схема таблиц на клиенте и сервере максимально одинаковая (`id`, `updated_at`, `deleted_at`). Конфликты в v1: **last-write-wins** по `updated_at` (один основной пользователь — ты). Два устройства одновременно без UI merge — не цель первой версии.

Web за компом с интернетом может ходить в API, но **кэш SQLite всё равно** нужен, если обещаем офлайн.

---

## 1. Принцип слоёв (как идём)

Не пилим XP, пока нельзя сохранить и открыть график без сети.

1. **Каркас** — слои кода, пустые экраны, память. *(частично уже в `ReactNative-version/frontend/`)*
2. **Рабочее ядро** — расписание + сегодня + правка + локальная SQLite.
3. **Облако** — Nest + Neon + логин + sync.
4. **Ассистент** — шаблоны + Groq через API.
5. **Продукт поверх ядра** — цели недели, закрытие дня, отчёты (без магазина).
6. **Геймификация** — XP, огонёк, магазин, косметика ассистента.
7. **MCP + полировка** — Cursor-tools, уведомления, uk, стор.

Каждый слой заканчивается **рабочей проверкой**, не «файлы появились».

---

## 1.1 Как возвращать UI (Flutter → Stitch → RN)

Референс вида: тёмный фон, лаванда, онбординг (Welcome / сферы), Today, Week, стекло-карточки, FAB ассистента. Сейчас это Flutter (`flutter-version/`) и скрины. Новые макеты делаем в **Stitch** (с кодом), в RN **не копируем HTML/React из Stitch целиком**.

### Что брать из Stitch

| Брать | Не брать в RN как есть |
|--------|-------------------------|
| Цвета, радиусы, отступы, типографика | сырой HTML / CSS / `div` |
| Структура экрана (шапка, список, таббар) | веб-`backdrop-filter` без проверки на native |
| Состояния кнопок, сетка сфер | отдельный роутинг Stitch |

Вынести в `ReactNative-version/frontend/src/ui/theme.ts` (или рядом): background, surface, accent (lavender), text, radius. Экраны импортируют тему, не hex из макета в каждом файле.

Строки в макетах могут быть **uk** — в приложении первой рабочей версии **EN**, uk в фазе 6. Макет = картинка; `l10n` = ключи.

### Когда какой кусок UI

| Когда | Что возвращаем | Готово когда |
|--------|----------------|--------------|
| **Фаза 1** (сейчас, параллельно ядру) | Только **токены**: тёмная тема, акцент, таббар не бежевый. Списки Today/Routine рабочие, без nebula и blur | Эмулятор/телефон: тёмный экран, читаемый текст, те же данные SQLite |
| **Конец фазы 1** | Каркас онбординга: 2–3 шага, Skip / Back / Next, без идеальной картинки | Можно пропустить и попасть в Today |
| **Фаза 4** | Today и Week «как в макете»: дата, чекбоксы, цели, полоска дней, модалка цели | Фичи фазы 4 работают **и** выглядят близко к Stitch |
| **Фаза 3** | Пузырь / кнопка ассистента по макету; чат простой | Не блокирует sync |
| **Фаза 5** | Shop в табах, профиль, косметика | После XP |
| **Фаза 6** | Стекло, glow-фон, uk, мелочь «как во Flutter» | Полировка, не блокер синка |

Flutter не портируем виджет за виджетом. Stitch — актуальный макет; Flutter — запасной референс, если Stitch ещё пустой.

### Правило

Сначала экран **делает работу** (сохранил пункт, отметил день), потом подтягиваем вёрстку из Stitch на **этот** экран. Не ждать полный набор макетов, чтобы начать SQLite. Не пилить Shop из Stitch, пока нет фазы 5.

---

## 2. Фаза 0 — Каркас (сейчас)

**Уже есть:** `ReactNative-version/frontend` shell, Today из SQLite, `evaluateDay` / economy как чистые функции, порты репозиториев.

**Добить в этой фазе (коротко):**

- [x] Папка `ReactNative-version/backend/` — NestJS hello + healthcheck (`GET /health`)
- [x] В README: три входа — RN, Nest, ссылка на этот план
- [x] `ARCHITECTURE.md`: data = sqlite + Nest/Neon, не Supabase

**Готово когда:** `npm start` в RN открывает shell; `npm run start:dev` в Nest отвечает на `/health`.

---

## 3. Фаза 1 — Рабочее ядро (самое важное)

Цель: **ты можешь вести график без облака.**

### Клиент

- [x] SQLite за `data/ports.ts` (сменить фабрику в `create-repos.ts`)
- [x] CRUD рутины: название, дни недели, время, сфера, усилие
- [x] Экран **Today**: список на дату, done / skipped / pending
- [x] Web: удобный редактор расписания (таблица/список, не только мобильный layout)
- [x] Данные переживают перезапуск приложения
- [x] Тема из Stitch: `theme.ts` (dark + lavender), без копипаста HTML

### Пока не трогаем

Закрытие дня, XP, магазин, Groq, Neon, MCP.

**Готово когда:** создал 5–10 пунктов как на своём скриншоте графика → увидел их сегодня → отметил → закрыл приложение → открыл снова, всё на месте, **без интернета**.

---

## 4. Фаза 2 — Nest + Neon + sync

Цель: **телефон и браузер сходятся в одной облачной БД**, офлайн не ломается.

### Сервер

- [x] Nest: модули `Auth`, `Routine`, `DayState`
- [x] Postgres на Neon, миграции (Prisma или TypeORM — один инструмент, не оба)
- [x] Регистрация/логин (email+password достаточно для себя)
- [x] Все строки с `user_id`; чужое не читается
- [x] API: pull с `since=updated_at`, push пачки изменений

### Клиент

- [x] Очередь исходящих правок в SQLite
- [x] При появлении сети — flush + pull
- [x] Индикатор «offline / syncing / synced» (минимальный)

**Готово когда:** правка на web → на телефоне после сети то же; правка офлайн на телефоне → после wifi появляется в Neon (проверить SQL editor Neon).

---

## 5. Фаза 3 — Ассистент (лайтовый)

- [x] Локальные шаблоны (уже идея Flutter `assistant_picker`) — всегда
- [x] `POST /assistant/chat` на Nest: контекст = сегодняшние пункты, не вся БД
- [x] Groq key только в env сервера; rate limit на user
- [x] UI: bubble / короткий чат; 429 или нет сети → шаблон, не пустой краш
- [x] История чата не обязательна в облаке

**Готово когда:** с телефона с сетью задаёшь «что у меня сегодня» и получаешь ответ по **реальным** пунктам SQLite/Postgres; без сети — живая заготовка.

**Любой день (не только today):** бэкенд уже принимает `dayKey`. В фазе 3 клиент всегда шлёт календарное сегодня. Расширение — **после фазы 4 или вместе с MCP (фаза 6)**, не блокер ядра недели:

- UI: день с экрана Today (стрелки) **или** выбор даты в чате.
- Тот же `POST /assistant/chat` с другим `dayKey` — вчера / завтра / любой день шаблона.
- MCP: не только `get_today`, а `get_day(date)` и `get_week_schedule` — спрашивать прошлое и будущее из Cursor.

Не грузить в Groq всю историю сразу: один день или одна неделя в контексте, как сейчас.

---

## 6. Фаза 4 — Продукт поверх графика

Только после фаз 1–2 (3 можно параллельно с 4, если API уже живой).

- [ ] Цели недели (CRUD + прогресс)
- [ ] Закрыть день: форма → `evaluateDay` → `DailyReport` в SQLite и sync
- [ ] Полоска качества дней на Week
- [ ] Короткий недельный отчёт (агрегат + 2 поля рефлексии)

XP **можно считать внутри закрытия дня и не показывать** — или показать число без магазина. Магазин — фаза 5.

**Готово когда:** неделя живёт как в MASTER_PLAN §4.2–4.3, без косметики.

---

## 7. Фаза 5 — Геймификация

- [ ] XP, капы, уровень (конфиг `economy`)
- [ ] Огонёк + предупреждения (MASTER_PLAN §8)
- [ ] Магазин за XP, экипировка, слои ассистента
- [ ] Профиль

**Готово когда:** закрыл зелёный день → XP и огонёк на профиле → купил рамку.

---

## 8. Фаза 6 — MCP, уведомления, полировка

- [ ] `steadyweek-mcp`: `get_week_schedule`, `get_day(date)`, `upsert_routine_item`, `get_today` → Nest (не прямой Neon service role из Cursor без auth). Любой день, не только today.
- [ ] Локальные напоминания «закрыть день»
- [ ] EN строки везде; uk следом (онбординг как в Stitch, но ключи l10n)
- [ ] Визуальная полировка по Stitch: glow-фон, glass, отступы «как на макете»
- [ ] Иконка, описание, если понадобится стор

**Готово когда:** из Cursor меняешь пункт рутины и после sync он на телефоне.

---

## 9. Порядок репозитория (папки)

```
SteadyWeek/
  docs/MASTER_PLAN.md     продукт (что)
  docs/DEV_PLAN.md        этот файл (как и когда)
  docs/stitch/            опционально: ссылки/экспорт макетов Stitch (не RN-код)
  ReactNative-version/
    frontend/             Expo клиент
    backend/              NestJS
  flutter-version/        Flutter, не трогаем без нужды
```

Клиент не знает SQL Neon. Nest не знает React. `evaluateDay` не знает HTTP.

---

## 10. Что делать следующим коммитом работы

Фаза 3 по коду закрыта. Дальше: **фаза 4 (цели недели, закрытие дня, отчёты)**.

Не начинать магазин и XP-полировку, пока не прогнан sync web ↔ phone на живом Neon.

Конфликт «хочу сразу AI» закрывается шаблонами ассистента на фазе 1 и Groq на фазе 3.
